# myvimrc
My .vimrc

```
git clone https://github.com/lleirborras/myvimrc.git ~/.myvimrc
rm ~/.vim
ln -s ~/.myvimrc ~/.vim
rm ~/.vimrc
ln -s ~/.myvimrc/vimrc ~/.vimrc
git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim
vim +PluginInstall +qall
```
